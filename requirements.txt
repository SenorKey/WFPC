# Python packages (pip install -r requirements.txt)
rapidocr-onnxruntime
numpy
Pillow
mss
requests

# RapidOCR bundles its ONNX models and runs on onnxruntime (pulled in
# automatically). No external system OCR engine is required — Tesseract
# is no longer used.